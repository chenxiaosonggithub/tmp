From e7248caa20cfd03bb89ef8a4147c63e43bf187f6 Mon Sep 17 00:00:00 2001
From: ChenXiaoSong <chenxiaosong@kylinos.cn>
Date: Thu, 18 Jun 2026 10:44:01 +0800
Subject: [PATCH] smb/client: smb3_notify_lazy_open

Signed-off-by: ChenXiaoSong <chenxiaosong@kylinos.cn>
---
 fs/smb/client/smb2ops.c | 144 +++++++++++++++++++++++++++++-----------
 1 file changed, 106 insertions(+), 38 deletions(-)

diff --git a/fs/smb/client/smb2ops.c b/fs/smb/client/smb2ops.c
index a8f8feeeccb5..e778661d19bf 100644
--- a/fs/smb/client/smb2ops.c
+++ b/fs/smb/client/smb2ops.c
@@ -2367,72 +2367,143 @@ smb3_enum_snapshots(const unsigned int xid, struct cifs_tcon *tcon,
 
 
 static int
-smb3_notify(const unsigned int xid, struct file *pfile,
-	    void __user *ioc_buf, bool return_changes)
+smb3_notify_lazy_open(const unsigned int xid, struct file *pfile)
 {
-	struct smb3_notify_info notify;
-	struct smb3_notify_info __user *pnotify_buf;
-	struct dentry *dentry = pfile->f_path.dentry;
 	struct inode *inode = file_inode(pfile);
 	struct cifs_sb_info *cifs_sb = CIFS_SB(inode->i_sb);
-	struct cifs_open_parms oparms;
-	struct cifs_fid fid;
+	struct cifsFileInfo *cfile, *existing;
 	struct cifs_tcon *tcon;
-	const unsigned char *path;
-	char *returned_ioctl_info = NULL;
-	void *page = alloc_dentry_path();
+	struct tcon_link *tlink = NULL;
+	struct cifs_open_parms oparms;
+	struct cifs_fid fid = {};
+	const char *path;
+	void *page = NULL;
 	__le16 *utf16_path = NULL;
 	u8 oplock = SMB2_OPLOCK_LEVEL_NONE;
-	int rc = 0;
-	__u32 ret_len = 0;
+	int rc;
 
-	path = build_path_from_dentry(dentry, page);
+	spin_lock(&inode->i_lock);
+	existing = pfile->private_data;
+	if (existing && !existing->invalidHandle) {
+		spin_unlock(&inode->i_lock);
+		rc = 0;
+		goto out;
+	}
+	if (existing && existing->invalidHandle) {
+		spin_unlock(&inode->i_lock);
+		rc = -EBADF;
+		goto out;
+	}
+	spin_unlock(&inode->i_lock);
+
+	tlink = cifs_sb_tlink(cifs_sb);
+	if (IS_ERR(tlink)) {
+		rc = PTR_ERR(tlink);
+		goto out;
+	}
+	tcon = tlink_tcon(tlink);
+
+	page = alloc_dentry_path();
+	path = build_path_from_dentry(pfile->f_path.dentry, page);
 	if (IS_ERR(path)) {
 		rc = PTR_ERR(path);
-		goto notify_exit;
+		goto out;
 	}
 
 	utf16_path = cifs_convert_path_to_utf16(path, cifs_sb);
-	if (utf16_path == NULL) {
+	if (!utf16_path) {
 		rc = -ENOMEM;
-		goto notify_exit;
+		goto out;
 	}
 
-	if (return_changes) {
-		if (copy_from_user(&notify, ioc_buf, sizeof(struct smb3_notify_info))) {
-			rc = -EFAULT;
-			goto notify_exit;
-		}
-	} else {
-		if (copy_from_user(&notify, ioc_buf, sizeof(struct smb3_notify))) {
-			rc = -EFAULT;
-			goto notify_exit;
-		}
-		notify.data_len = 0;
+	cfile = kzalloc_obj(struct cifsFileInfo);
+	if (!cfile) {
+		rc = -ENOMEM;
+		goto out;
 	}
+	spin_lock_init(&cfile->file_info_lock);
+	cfile->tlink = cifs_get_tlink(tlink);
 
-	tcon = cifs_sb_master_tcon(cifs_sb);
 	oparms = (struct cifs_open_parms) {
 		.tcon = tcon,
 		.path = path,
 		.desired_access = FILE_READ_ATTRIBUTES | FILE_READ_DATA,
 		.disposition = FILE_OPEN,
-		.create_options = cifs_create_options(cifs_sb, 0),
+		.create_options = cifs_create_options(cifs_sb, CREATE_NOT_FILE),
 		.fid = &fid,
 	};
 
 	rc = SMB2_open(xid, &oparms, utf16_path, &oplock, NULL, NULL, NULL,
 		       NULL);
-	if (rc)
-		goto notify_exit;
+	if (rc) {
+		cifs_put_tlink(cfile->tlink);
+		kfree(cfile);
+		goto out;
+	}
+
+	cfile->fid = fid;
+	cfile->invalidHandle = false;
 
-	rc = SMB2_change_notify(xid, tcon, fid.persistent_fid, fid.volatile_fid,
+	spin_lock(&inode->i_lock);
+	existing = pfile->private_data;
+	if (existing) {
+		spin_unlock(&inode->i_lock);
+		SMB2_close(xid, tcon, fid.persistent_fid, fid.volatile_fid);
+		cifs_put_tlink(cfile->tlink);
+		kfree(cfile);
+		if (!existing->invalidHandle)
+			rc = 0;
+		else
+			rc = -EBADF;
+		goto out;
+	}
+	pfile->private_data = cfile;
+	spin_unlock(&inode->i_lock);
+	rc = 0;
+
+out:
+	kfree(utf16_path);
+	free_dentry_path(page);
+	cifs_put_tlink(tlink);
+	return rc;
+}
+
+static int
+smb3_notify(const unsigned int xid, struct file *pfile,
+	    void __user *ioc_buf, bool return_changes)
+{
+	struct smb3_notify_info notify;
+	struct smb3_notify_info __user *pnotify_buf;
+	struct cifsFileInfo *cfile = pfile->private_data;
+	struct cifs_tcon *tcon;
+	char *returned_ioctl_info = NULL;
+	int rc = 0;
+	__u32 ret_len = 0;
+
+	if (!cfile || cfile->invalidHandle) {
+		rc = smb3_notify_lazy_open(xid, pfile);
+		if (rc)
+			return rc;
+		cfile = pfile->private_data;
+	}
+
+	if (return_changes) {
+		if (copy_from_user(&notify, ioc_buf, sizeof(struct smb3_notify_info)))
+			return -EFAULT;
+	} else {
+		if (copy_from_user(&notify, ioc_buf, sizeof(struct smb3_notify)))
+			return -EFAULT;
+		notify.data_len = 0;
+	}
+
+	tcon = tlink_tcon(cfile->tlink);
+
+	rc = SMB2_change_notify(xid, tcon, cfile->fid.persistent_fid,
+				cfile->fid.volatile_fid,
 				notify.watch_tree, notify.completion_filter,
 				notify.data_len, &returned_ioctl_info, &ret_len);
 
-	SMB2_close(xid, tcon, fid.persistent_fid, fid.volatile_fid);
-
-	cifs_dbg(FYI, "change notify for path %s rc %d\n", path, rc);
+	cifs_dbg(FYI, "change notify rc %d\n", rc);
 	if (return_changes && (ret_len > 0) && (notify.data_len > 0)) {
 		if (ret_len > notify.data_len)
 			ret_len = notify.data_len;
@@ -2443,9 +2514,6 @@ smb3_notify(const unsigned int xid, struct file *pfile,
 			rc = -EFAULT;
 	}
 	kfree(returned_ioctl_info);
-notify_exit:
-	free_dentry_path(page);
-	kfree(utf16_path);
 	return rc;
 }
 
-- 
2.54.0

